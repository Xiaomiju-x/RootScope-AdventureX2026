# RootScope v3 E0 交接

日期：2026-07-24  
状态：`E0_COMPLETE_PC_ONLY_X5_PENDING`

## 结论

E0 已在完全独立的 `adventurex/rootscope_v3/` 工作根完成。没有覆盖
`rootscope/` v2，没有连接断电的 X5，没有读取密钥值，也没有打开相机、
串口、GPIO 或泵。

v2 tar 重新计算后仍为：

`03ca7b8d9ff8b691f1fd61dc696601ba30f494377a0b2a3cfadb66c19478ed94`

## 冻结输出

- 事实与增量：
  `governance/baseline_v2_snapshot.json`、`governance/PREEXISTING.md`、
  `governance/BUILT_DURING_EVENT.md`
- 四类注册表：
  `registries/models.v1.json`、`registries/datasets.v1.json`、
  `registries/teachers.v1.json`、`registries/dependencies.v1.json`
- 五套评测合同：
  `schemas/evaluation/vision_evaluation.schema.json`、
  `llm_evaluation.schema.json`、`rag_evaluation.schema.json`、
  `resource_evaluation.schema.json`、`physical_loop_evaluation.schema.json`
- 最小合法示例：`examples/evaluation/`
- 不可部署候选骨架：
  `candidates/rootscope_v3_candidate_unqualified/`
- 自动门禁：`tools/verify_e0.py`、`tests/test_e0_contracts.py`
- 回执：`evidence/e0_verification_receipt_20260724.json`

注册表与 schema 的冻结组合根为：

`43882938b7bb3ef34b8febf51ac1a8bbc92c8cc815e848b8b5c61d371768eaa3`

## 验证结果

```powershell
.\.ai_curation_venv\Scripts\python.exe -m unittest discover -s rootscope_v3\tests -v
# 7 tests, OK

.\.ai_curation_venv\Scripts\python.exe rootscope_v3\tools\verify_e0.py --adventurex-root .
# 345 checks
# PASS_E0_FACTS_REGISTRIES_SCHEMAS_CANDIDATE_ZERO_AUTHORITY
```

## 后续工作合同

- 后续视觉、LLM、RAG、资源和物理工作必须写出对应 schema 的结果；
- 新下载或训练模型先追加 registry，只有真实文件存在后才能填 SHA-256；
- 教师调用只允许引用注册表中的环境变量名，禁止保存或打印密钥值；
- `rootscope_v3_candidate_unqualified` 永远不是 release；
- 正式 v3 candidate 必须创建新 immutable id，并继续绑定 v2 回滚哈希；
- X5 上电前所有资格状态只能是 `PC_ONLY`、`FIXTURE_ONLY` 或
  `X5_PENDING`。

本工作根与并行新增的 `rootscope/app/runtime_v3/`、
`rootscope/app/action_v3/`、`rootscope/configs/competition_v3/` 分离，
没有修改或覆盖这些路径。后续打包器可以显式把通过各自测试的模块复制
进新的不可变 candidate。
